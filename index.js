const Build = require('./lib/Build');

// Create new named instance 'architect' of modeled object "Build" as imported module
const architect = new Build();

// Execute "Build" object method 'start()' to initiate prompt sequence as Object encapsulated
architect.start();